<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
	xmlns:cells="clr-namespace:CSAssistant.Cells;assembly=CSAssistant"
	x:Class="CSAssistant.Pages.CoursePage"
	Title="{Binding Title}" IsBusy="{Binding ShowLoading}">
	<ContentPage.Content>
		<StackLayout>
			<ActivityIndicator IsRunning="{Binding ShowLoading}" IsVisible="{Binding ShowLoading}"
				HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand" Scale="2"/>
			<ListView ItemsSource="{Binding Assignments}" IsVisible="{Binding ShowContent}" SelectedItem="{Binding Selection, Mode=TwoWay}">
				<ListView.ItemTemplate>
					<DataTemplate>
						<cells:RightArrowCell Text="{Binding Name}" />
					</DataTemplate>
				</ListView.ItemTemplate>
			</ListView>
		</StackLayout>
	</ContentPage.Content>
</ContentPage>
